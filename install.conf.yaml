- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    #~/.vim: vim
    # Vim
    ~/.vimrc: vim/vimrc

    # Zsh
    ~/.zprofile:
        path: zsh/zprofile
    ~/.zshenv:
        path: zsh/zshenv
    ~/.zshrc:
        path: zsh/zshrc
    ~/.zlogout:
        path: zsh/zlogout

    # Bash

    # Git
    ~/.config/git:
        create: true

    # Bat
    ~/.config/bat:
        create: true
        path: bat

    # Tmux
    ~/.tmux:

- create:
    - ~/Developer

# - shell:
#     #- [git submodule update --init --recursive, Installing submodules]
#     - 
#       command: ./setup\ scripts/setup_homebrew.zsh
#       stdout: true
#       stdin: true
#       stderr: true

