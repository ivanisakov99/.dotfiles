- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    #~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Developer

- shell:
    #- [git submodule update --init --recursive, Installing submodules]
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true

